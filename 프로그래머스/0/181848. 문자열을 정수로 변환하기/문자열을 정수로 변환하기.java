class Solution {
    public int solution(String n_str) {
        int answer = 0;
        
        // 문자열 -> 정수로 형변환 하기 (Integer.parseInt() 사용)
        answer = Integer.parseInt(n_str);
        
        return answer;
    }
}